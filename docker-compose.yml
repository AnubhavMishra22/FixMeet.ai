services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: fixmeet
      POSTGRES_PASSWORD: fixmeet
      POSTGRES_DB: fixmeet
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
